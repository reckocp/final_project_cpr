<%= form_for [Conversation.new] do |f| %>
  <div class="field">
    <%= f.hidden_field :user_id, :value => current_user %>
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit "Send" %>
  </div>
<% end %>
